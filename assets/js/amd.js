(function (root, factory) {
    if (typeof define === 'function' && define.amd) {
        // AMD. Register as an anonymous module.
        define(["jQuery"], factory);
    }
}(this, function (jQuery) {
  //= ../../dist/revision/js/app.js
}));